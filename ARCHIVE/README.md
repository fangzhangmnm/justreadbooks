# ARCHIVE —— v1（vanilla JS，≤ v23 / 2026-09-01）只读参考

> created 2026-09-19 by Claude Fable 5.1。JRB 0.1 纪元按 `ai-docs/20260919-modernization-plan.md` 在 TS + @internal/store + @internal/gallery 上重做；
> 旧代码整体 `git mv` 到这里**只当算法 / UX spec 读**（切章正则、编码探测、手柄 D-pad 量化、象牙白金主题 token），不 type、不 import、不 build。
> 线上 `/`（prod 分支）仍是这份 v1，直到真机总单过、user 说 push prod。pdf.js vendor 也在这里（PDF 下一轮出 `@internal/pdf-viewer` 控件时再取）。
